package org.firstinspires.ftc.teamcode.trialsystems;

import com.qualcomm.robotcore.eventloop.opmode.LinearOpMode;
import org.firstinspires.ftc.teamcode.systems.CameraView;

import java.util.stream.Collectors;

/*public class aprilTagShooter extends LinearOpMode {
    int blueGoalID = 20;
    int redGoalID = 24;
    int alliance = 0; // 0 is Blue Alliance, while 1 is Red Alliance

    public void runOpMode() {
        CameraView.initScanner();

        waitForStart();

        if (opModeIsActive()) {
            while (opModeIsActive()) {
                if (aprilTag.getDetections() != null && !aprilTag.getDetections().isEmpty()) {
                    telemetry.addData("Tag IDs", aprilTag.getDetections().stream()
                            .map(d -> d.id)
                            .collect(Collectors.toList()).toString());
                } else {
                    telemetry.addLine("No tags detected");
                }
                telemetry.update();

            }

        }

    }

    public void


} */
